services:
  frontend:
    build: .
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "4200:4200"
    environment:
      - NG_CLI_ANALYTICS=ci
    command: ng serve --host 0.0.0.0 --poll 2000
    stdin_open: true
    tty: true