services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: angular_nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro

